# Git
.git
.gitignore
.github

# Environment and secrets (never in image)
.env
.env.*
!.env.example

# Python virtual envs
.venv
venv
env
.env

# Bytecode and caches
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg-info
*.egg
.pytest_cache
.mypy_cache
.ruff_cache

# Build / dist (not needed in image)
build
dist
*.egg

# Django local / generated
*.log
db.sqlite3
*.sqlite3
staticfiles
media
local_settings.py

# Tests (exclude from prod image to reduce size)
tests
.coverage
.coverage.*
htmlcov
.tox
nosetests.xml
coverage.xml

# IDE and OS
.idea
.vscode
.cursor
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Docker (not needed inside image)
Dockerfile
docker-compose.yml
docker-compose.*.yml
.dockerignore

# Docs and tooling (optional — uncomment if you want minimal image)
# README.md
# Makefile

# Dependencies: image uses requirements.txt; lockfiles not needed in build context
poetry.lock
pyproject.toml
